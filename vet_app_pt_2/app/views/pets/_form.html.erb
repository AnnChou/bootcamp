<%= form_for @pet do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div>
    <%= f.label :species %>
    <%= f.text_field :species %>
  </div>
  <div>
    <%= f.label :breed %>
    <%= f.text_field :breed %>
  </div>
  <div>
    <%= f.label :owner %>
    <%= f.select :owner_id, options_from_collection_for_select(Owner.all, 'id', 'name') %>
  <div>
    <%= f.submit %>
  </div>
<% end %>
